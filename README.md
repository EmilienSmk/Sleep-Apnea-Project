source ~/apnea_env/bin/activate Change to the enviornment to run the python programs.
Run "rm -rf /tmp/spark_checkpoint" before running spark_processor.py to prevent checksum errors from incorrect closure of the program.

